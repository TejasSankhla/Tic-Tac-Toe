import { StrictMode } from "react";
import { createRoot } from "react-dom/client";
import App from "./App.jsx";
import "./index.css";

createRoot(document.getElementById("root")).render(
  <StrictMode>
    <App />
  </StrictMode>
);

// React Interview topics
// How react wprks -> DOM TREE
// state ? hooks
// jsx
// props
// prop drilling
// to remove prop_drilling -> useContext or use Redux
// hooks -> useState, useEffect, useRef, useMemo, useCallback

// BAckend
// Database
// SQL 
// NoSQL -> MongoDB
// why did you use this database
// JS -> lang based question promise, async await
// REst aPI , CRUD operstinons , schema design

